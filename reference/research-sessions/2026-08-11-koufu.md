# Research Session — Koufu (2026-08-11)

**Track:** restaurant / food_court / hawker / coffeeshop / canteen (automated scheduled run)

## Selection

Filtered `RESEARCH_QUEUE` to `status: 'pending'` entries with `type` in
`restaurant`/`food_court`/`hawker`/`coffeeshop`/`canteen` (excluding
`grab_go`/`ready_to_eat`/`supermarket`, which belong to the sibling
grocery-track task). Sorted by priority (high → medium → low), keeping
array order within each tier. Three `high`-priority `food_court` entries
remained pending (`koufu`, `foodfare`, `banquet`); `kopitiam` — previously
tied with them — was already researched in the prior run. Per the
deterministic tie-break rule (first-listed wins), **`koufu`** was selected.

Confirmed `id: "koufu"` was not already present in `outlets.ts` — only the
pre-existing per-stall archetypes (`koufu_chicken_rice_stall`,
`koufu_mixed_veg_rice_stall`, `koufu_wonton_noodle_stall`,
`koufu_laksa_stall`, `koufu_nasi_lemak_stall`, `koufu_ban_mian_stall`,
`koufu_char_kway_teow_stall`, `koufu_drinks_stall`) existed, none sharing
the bare `koufu` id.

## Outlet

Added `Outlet` id `koufu` (`type: 'food_court'`, `cuisine: 'Food Court'`).
Koufu Pte Ltd is a Singapore food-court/coffee-shop operator founded in
2002 by Pang Lim, operating under multiple brands with outlets across
Singapore (and one in Macau) — source: Wikipedia "Koufu (company)" and
Koufu's own corporate site.

Per task instructions, `type: 'food_court'` is an operator brand not tied
to a single physical premises (Koufu is explicitly named as an example in
the task spec itself), so **SFA lookup was skipped entirely** — no `sfa`
block was added.

## Menu items

Existing per-stall archetypes already cover chicken rice, mixed veg rice
(cai png), wonton noodle, laksa, nasi lemak, ban mian, char kway teow, and
drinks. To avoid duplicating those, the 8 new `FoodOption` records (linked
to `outletId: "koufu"`) cover dish categories not yet represented under the
Koufu brand: breakfast items and additional noodle/snack hawker staples.

| Item | Confidence | Basis |
|---|---|---|
| Traditional Kaya Toast Set | estimated | Kaya toast 2-slice serving (~390 kcal/13g protein per Lemon8/SnapCalorie aggregation) + 2 soft-boiled eggs (~140 kcal/12g protein) + kopi (~120 kcal) |
| Roti Prata (Plain, 2pc) w/ Curry | estimated | Per-piece figures (~209 kcal, 5g protein, 7g fat, 32g carbs — SnapCalorie/SingaporeCalorie) ×2 + dhal curry estimate |
| Hokkien Mee (Prawn Noodle) | estimated | Mid-range of SnapCalorie/SingaporeCalorie/Nutritionix hawker figures (~500-650 kcal per 400g serving, ~22-26g protein) |
| Bak Chor Mee (Dry) | estimated | SingaporeCalorie/DanielFoodDiary hawker figures (~350-460 kcal per bowl, 20-30g protein) |
| Chwee Kueh (5pc) | estimated | SingaporeCalorie/CalorAI consistent figures (~320-330 kcal per 5pc, 6g protein) |
| Popiah (2 rolls) | estimated | SingaporeCalorie/SnapCalorie per-roll figures (~188-200 kcal, 6-8g protein per roll) ×2 |
| Yong Tau Foo (Noodle Soup, Mixed) | estimated | Arise App/SingaporeCalorie mixed soup-with-noodle figures (~350-420 kcal, 20-25g protein) |
| Fishball Noodle Soup | estimated | SingaporeCalorie/noah soup-version figures (~330-420 kcal range, 18-20g protein) |

No item reached `'verified'` confidence — Koufu is a multi-stall food court
operator, not a single brand with its own published nutrition PDF, and no
exact HPB Nutrition Hub match was queried directly (all figures sourced
from secondary aggregator sites: SnapCalorie, SingaporeCalorie, CalorAI,
Arise App, Lemon8, noah, DanielFoodDiary, Nutritionix). None were
downgraded to `'community'` since each is a real, named hawker dish with a
credible sourced estimate rather than a generic placeholder value.

**Item count:** 8 (within the 6-15 target range).
**Confidence breakdown:** 8 estimated, 0 verified, 0 community.

## SFA registration

Not applicable — `type: 'food_court'` per task Phase 3 (operator brand,
explicitly named as a skip-case example in the task spec).

## Typecheck

Copied project (excluding `node_modules`, `.next`, `out`, `.git`,
`reference`) to a sandbox dir, ran `npm install` (394 packages, clean) then
`npx tsc --noEmit`. **Result: passed, no errors.** No type annotation was
added to the `OUTLETS`/`FOOD_OPTIONS` array exports.

## Queue

`koufu` entry in `researchQueue.ts` flipped from `'pending'` to
`'researched'`.

## Files changed

- `src/lib/outlets.ts` — added `koufu` Outlet
- `src/lib/foodOptions.ts` — added 8 FoodOption records
- `src/lib/researchQueue.ts` — `koufu` status → `researched`
