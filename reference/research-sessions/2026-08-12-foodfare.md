# Research session: Foodfare — 2026-08-12

**Track:** restaurants / food court / hawker / coffeeshop / canteen
**Queue entry selected:** `foodfare` (priority: high, first-listed among high-priority pending matches — the other high-priority candidate, `banquet`, was listed after it in the array)

## Outlet

- id: `foodfare`
- name: Foodfare
- type: `food_court` (NTUC-operated food court/coffeeshop operator — not a single physical premises)
- cuisine: Food Court
- aliases: `foodfare`, `ntuc foodfare`
- No `location`, no `dietTags` at outlet level, `priceRange: "$"`, `platforms: ["dine_in", "grab_go"]` — matches the existing pattern for sibling operator entries `kopitiam` and `koufu`.

## Sources

- [Foodfare @ KTPH: New Look, New Serves](https://www.ntuc.org.sg/uportal/news/Foodfare--KTPH-New-Look-New-Serves/) — confirms calorie labelling on menu boards/receipts, healthier-choice pricing incentives (brown rice, less-sugar drinks), but no per-dish nutrition table.
- [NTUC Foodfare — official site](https://www.foodfare.com.sg/) — outlet listing, no published nutrition database.
- [Singapore Food Insights Database — Health Promotion Board](https://www.hpb.gov.sg/healthy-living/food-and-beverage/sgfoodid/) — generic nutrient composition reference for common Singapore dishes.
- [Singapore Hawker Food Nutrition Database — NutriKaki](https://nutrikaki.com/nutrition/) — secondary reference sourced from HPB food composition data, used to sanity-check macro ranges for common coffeeshop dishes (chicken rice, laksa, mee siam, wonton mee, carrot cake, etc.).

No Foodfare-specific per-dish nutrition PDF or page was found (consistent with the queue entry's note: "Use HPB nutrient data" — same instruction and same outcome as the earlier Kopitiam/Koufu research sessions). Foodfare, like Kopitiam and Koufu, is a multi-location operator brand, not a single premises with one fixed menu — so per the researcher instructions this is a generic-dish approach, not a false claim of outlet-verified data.

## Items added (8)

All items use **generic HPB/NutriKaki-style hawker-dish nutrient estimation**, the same methodology already used for the `kopitiam` and `koufu` operator entries in this database. None of these figures are Foodfare-specific verified values — confidence is `estimated` throughout, deliberately not `verified`.

| Item | Category | Price | kcal | Protein | Carbs | Fat |
|---|---|---|---|---|---|---|
| Economy Mixed Vegetable Rice (3 Dishes) | Rice | $4.00 | 550 | 20g | 65g | 20g |
| Chicken Rice | Rice | $4.50 | 600 | 30g | 65g | 22g |
| Laksa | Noodles | $5.50 | 600 | 18g | 55g | 35g |
| Mee Siam | Noodles | $4.00 | 420 | 10g | 68g | 12g |
| Wonton Noodle (Dry) | Noodles | $4.50 | 420 | 20g | 58g | 12g |
| Fried Carrot Cake (Black) | Snacks | $3.50 | 480 | 8g | 60g | 22g |
| Curry Chicken with Rice | Rice | $5.00 | 620 | 28g | 60g | 28g |
| Soya Bean Curd (Tau Huay) | Desserts | $1.80 | 120 | 6g | 18g | 3g |

Dish selection deliberately overlaps only partially with Kopitiam/Koufu's existing generic lists (e.g. laksa, mee siam, carrot cake, curry chicken rice, and tau huay are new; some overlap with hawker-fare staples is expected and acceptable across sibling operator entries per the task instructions). No item was fabricated without a credible generic basis — all are well-documented, common Singapore coffeeshop/food-court dishes with well-established typical macro ranges.

`compatibleWith` tags were only added where safely inferable from the dish's known composition (e.g. tau huay → vegetarian + lactose_free; dry noodle dishes with no dairy → lactose_free). Mixed-dish rice items and dishes with ambiguous meat/halal status were left with an empty `compatibleWith` array rather than guessed.

## Confidence breakdown

- verified: 0
- estimated: 8
- community: 0

## SFA registration

Not attempted. Per Phase 3 instructions, `type: 'food_court'` operator-brand entries (Foodfare runs 15+ locations, not tied to one premises) are explicitly out of scope for SFA lookup — `sfa` left unset, matching the existing `kopitiam`/`koufu` entries.

## Build verification

Copied project (excluding `node_modules`, `.next`, `out`, `.git`, `reference`) to a sandbox, ran `npm install`, then `npx tsc --noEmit`.

**Result: PASS** — no type errors. `OUTLETS`/`FOOD_OPTIONS` array exports were not given type annotations (kept as untyped literals per the TS2590 workaround already in place).

## Queue status

`researchQueue.ts` entry `foodfare`: `status` flipped from `pending` → `researched`, with a note appended documenting this session.

## Git commit — NOT completed (environment issue, pre-existing)

`git add -A` failed:

```
fatal: Unable to create '.../.git/index.lock': File exists.
```

`.git/index.lock` and `.git/HEAD.lock` are stale files left over from a crashed git process, but this session could not remove them (`rm`, `mv`, and `chmod` all returned `Operation not permitted` despite the files being owned by the current user — the filesystem itself is refusing deletion, not a git-level issue). `lsattr` also failed ("Operation not supported"), so the cause couldn't be further diagnosed from this sandbox.

This is a **recurring, pre-existing problem**, not something introduced by this session — see `reference/stale-locks-2026-08-10/` and `reference/stale-locks-2026-08-11/` for evidence that the two prior scheduled runs (2026-08-10, 2026-08-11) hit the identical blocker and were also unable to clear it (multiple `.bak`/`.bak2`/`.bak3` removal attempts visible in the 08-10 folder).

No destructive workaround (manually rewriting `.git/index`, forcing an alternate `GIT_INDEX_FILE`, etc.) was attempted, since that risks corrupting the repo further in an unattended run.

**All file changes are saved and typechecked successfully** — only the `git commit` step is blocked. Files changed this session: `src/lib/outlets.ts`, `src/lib/foodOptions.ts`, `src/lib/researchQueue.ts`, plus this report. **A human needs to manually delete `.git/index.lock` and `.git/HEAD.lock`** (likely requires doing so outside this sandboxed environment, e.g. directly in Windows Explorer or a native terminal) before any commit — by this session or a future one — can succeed.
