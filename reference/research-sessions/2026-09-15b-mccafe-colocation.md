# 2026-09-15b — Scheduled grocery-track run (20th pass on `mccafe_colocation_research`, same-day duplicate)

**Track:** `grab_go` / `ready_to_eat` / `supermarket`
**Outcome:** No new Brand/MenuItem/GroceryProduct/Premises data added. Queue entry left `'pending'`. Appended a one-line 20th-pass reconfirmation to the entry's `notes` field and committed it (`35e84dd`) — no build/typecheck needed since no data table changed.

## Queue scan (Phase 1)

Parsed `src/lib/researchQueue.ts` programmatically (131 entries; verified via a small Node eval rather than eyeballing the array). Filtered to `status: 'pending'` AND `type` in `{grab_go, ready_to_eat, supermarket}`.

**Result: exactly one match** — `mccafe_colocation_research` (`type: grab_go`, `priority: medium`). Every other in-scope entry (`coffeesmith`, `hollin`, `bengawan_solo`, `ok_convenience`, and others) is `'researched'`. This is the 20th consecutive scheduled run to land on this same candidate, and the second run today — a report for this same entry (`2026-09-15-mccafe-colocation.md`, "19th pass") was already logged earlier today.

## Why nothing new was added this pass

Re-verified rather than re-investigated, per the standing discipline established over the last several passes:

- `brands.ts` — `mccafe` Brand still present, unchanged (2 occurrences: the object + its own alias self-reference).
- `menuItems.ts` — still exactly 10 `mccafe_*` MenuItem entries.
- `premises.ts` — still 0 rows for `brandId: "mccafe"`.
- `researchQueue.ts` — `ok_convenience` still `'researched'`; no other in-scope entries exist.
- `git status` — working tree was clean at the start of this run (the 19th pass's git-lock recovery had already been committed by other tracks since this morning; current `HEAD` was `9ec85b1`).

The underlying blocker is unchanged from every prior pass: this is a **schema/taxonomy decision**, not a research gap. The empirical question (does McCafé still operate in Singapore) was resolved back on 2026-08-31 — yes, via main-counter service since 27 Mar 2026, not dedicated McCafé corners, confirmed on `mcdonalds.com.sg/mccafe` ("Available at all restaurants islandwide"). What remains unresolved is a product call between:

- **(a)** copying all ~136–145 existing `mcdonalds` Premises rows as new `mccafe` Premises rows, or
- **(b)** folding `mccafe`'s 10 existing MenuItems into the `mcdonalds` Brand as a beverage category and dropping the standalone `mccafe` Brand.

Both restructure an existing, already-populated Brand — outside this task's normal per-entry scope. Not resolving it unilaterally, consistent with all 19 prior passes.

## Action taken this pass

Appended a single-line "UPDATE 2026-09-15b (20th pass...)" reconfirmation to the `mccafe_colocation_research` entry's `notes` field in `researchQueue.ts` (that field is now very large — ~19 dated updates — so kept this addition to one line, per the standing guidance left in the 17th-pass note). No other file touched.

## Repo hygiene note

`.git/index.lock` and `.git/HEAD.lock` were present at the start of this run (same recurring issue documented in the 19th-pass report and many earlier ones — `rm`/`unlink` is blocked on this mount, but `mv`/rename works). Moved both aside (`index.lock.stale-<timestamp>`, `HEAD.lock.stale-<timestamp>-e`) so `git` would function, then committed normally. The commit succeeded despite `unlink`-permission warnings during git's own post-commit lock cleanup (expected — same mount-level restriction, not a real error); a fresh `index.lock`/`HEAD.lock` pair was regenerated by git afterward as a side effect of that same restriction, which is why this keeps recurring across sessions. Did not attempt a deeper fix (e.g. deleting the large number of accumulated `.lock.*` backup/stale files in `.git/`) — that's a repo-hygiene cleanup outside this task's scope, not something blocking the grocery-track research this task is responsible for. Flagging again for a future `improve-app`-style session, as prior reports have.

## Phase 4 / 5

- **Typecheck:** skipped — no `Brand`/`MenuItem`/`GroceryProduct`/`Premises` data changed this pass.
- **Commit:** `35e84dd` — "Research: mccafe_colocation_research 20th-pass reconfirmation (2026-09-15, same-day duplicate)". Not pushed (per task rules, pushing is left to the user). Local `main` is now 4 commits ahead of `origin/main`.

## Recommendation (unchanged, now with a 20th run behind it)

Same as the 19th pass: a human should either make the (a)/(b) taxonomy call directly, or change this entry so automated runs stop re-selecting it — `ResearchQueueEntry.status` only supports `'pending' | 'researched'` (`src/types/db.ts`), with no schema-supported "blocked" state. Twenty consecutive identical outcomes (two of them same-day) confirm further unattended runs will not add new information; each additional run only adds a one-line audit-trail note now, not new facts.
