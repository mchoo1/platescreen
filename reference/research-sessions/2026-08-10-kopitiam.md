# Research Session — Kopitiam (2026-08-10)

**Track:** restaurant / food_court / hawker / coffeeshop / canteen (automated scheduled run)

## Selection

Filtered `RESEARCH_QUEUE` to `status: 'pending'` entries with `type` in
`restaurant`/`food_court`/`hawker`/`coffeeshop`/`canteen`. Four `high`-priority
food_court entries tied (`kopitiam`, `koufu`, `foodfare`, `banquet`); per the
deterministic tie-break rule (first-listed wins), **`kopitiam`** was selected.
Confirmed `id: "kopitiam"` was not already present in `outlets.ts` — only the
pre-existing per-stall archetypes (`kopitiam_chicken_rice_stall`,
`kopitiam_mixed_veg_rice_stall`, `kopitiam_noodle_stall`, `kopitiam_laksa_stall`,
`kopitiam_nasi_lemak_stall`, `kopitiam_western_stall`, `kopitiam_drinks_stall`)
existed, none sharing the bare `kopitiam` id.

## Outlet

Added `Outlet` id `kopitiam` (`type: 'food_court'`, `cuisine: 'Food Court'`).
Kopitiam Investment Pte Ltd is confirmed as a Singapore food-court operator
(under the NTUC FairPrice group, alongside NTUC Foodfare), founded 1988,
100+ outlets islandwide (Changi Airport, Plaza Singapura, VivoCity, Jem,
etc.) — source: Wikipedia "Kopitiam (company)".

Per task instructions, `type: 'food_court'` is an operator brand not tied to
a single physical premises, so **SFA lookup was skipped entirely** — no
`sfa` block was added.

## Menu items

Existing per-stall archetypes already cover chicken rice, mixed veg rice,
wonton/ban mian/fishball noodles, laksa/curry noodle, nasi lemak, Western
mains, and drinks (kopi/teh/milo). To avoid duplicating those, the 8 new
`FoodOption` records (linked to `outletId: "kopitiam"`) cover dish categories
not yet represented in the DB: breakfast items and additional
noodle/snack hawker staples commonly found at Kopitiam-operated food courts.

| Item | Confidence | Basis |
|---|---|---|
| Traditional Kaya Toast Set | estimated | Composite of kaya toast (lemon8: 2 slices ≈ 390 kcal / 13g protein), 2 soft-boiled eggs (~140 kcal / 12g protein), and a milk kopi (~120 kcal) |
| Roti Prata (Plain, 2pc) w/ Curry | estimated | SnapCalorie/SingaporeCalorie per-100g prata figures (200 kcal, 4g protein, 30g carbs, 8g fat per 100g) × 2 pieces + dhal curry estimate |
| Char Kway Teow | estimated | Mid-range of MyNetDiary/Arise App hawker-CKT figures (300g serving ~700-750 kcal range) |
| Hokkien Mee (Prawn Noodle) | estimated | Mid-range of SnapCalorie (500 kcal/400g, 25g protein) and recipe-based (830 kcal) sources |
| Bak Chor Mee (Dry) | estimated | SingaporeCalorie "Bak Chor Mee (Dry)" ~460 kcal/400g bowl |
| Chwee Kueh (5pc) | estimated | Mid-range of SnapCalorie/SingaporeCalorie figures (224-330 kcal per 4-5pc serving) |
| Popiah (2 rolls) | estimated | SnapCalorie fresh popiah, 188 kcal/roll × 2 |
| Yong Tau Foo (Noodle Soup, Mixed) | estimated | Arise App "Mixed Yong Tau Foo with Noodles" / SnapCalorie soup-with-noodle figures |

No item reached `'verified'` confidence — Kopitiam is a multi-stall food
court operator, not a single brand with its own published nutrition PDF, and
no exact HPB Nutrition Hub API match was queried (all figures sourced from
secondary aggregator sites: SnapCalorie, SingaporeCalorie, Arise App,
MyNetDiary, lemon8). None were downgraded to `'community'` since each is
outlet-category-specific (a real, named hawker dish) rather than a generic
placeholder value.

**Item count:** 8 (within the 6-15 target range).
**Confidence breakdown:** 8 estimated, 0 verified, 0 community.

## SFA registration

Not applicable — `type: 'food_court'` per task Phase 3 (operator brand,
skipped per instructions).

## Typecheck

Copied project (excluding `node_modules`, `.next`, `out`, `.git`,
`reference`) to a sandbox dir, ran `npm install` (394 packages, clean) then
`npx tsc --noEmit`. **Result: passed, no errors.** No type annotation was
added to the `OUTLETS`/`FOOD_OPTIONS` array exports.

## Queue

`kopitiam` entry in `researchQueue.ts` flipped from `'pending'` to
`'researched'`.

## Files changed

- `src/lib/outlets.ts` — added `kopitiam` Outlet
- `src/lib/foodOptions.ts` — added 8 FoodOption records
- `src/lib/researchQueue.ts` — `kopitiam` status → `researched`
