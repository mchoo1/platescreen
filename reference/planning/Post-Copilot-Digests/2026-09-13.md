# Post Copilot Digest — 2026-09-13

## Outcome: no post drafted this run

Checked every theme in `CONTENT_QUEUE.md` against fresh live data (loaded
directly from `src/lib/menuItems.ts` / `brands.ts` / `groceryProducts.ts` via
a scratch script — no build mirror existed in this sandbox, so one wasn't
synced; these files have zero imports and no TS-only syntax beyond
`@ts-nocheck`, so they were loaded as plain data with no type-stripping
needed, and no scratch files were left in the repo). None cleared the
"8-10 solid, publicly-defensible rows" bar. This is now the fourth
consecutive run (2026-08-31 partial success only, then 2026-09-03,
2026-09-07, and today) with essentially the same root cause. No draft was
written and no browser tab was opened — nothing to review or post from this
run.

## Live data snapshot

| Metric | Value |
|---|---|
| Brands | 1,724 |
| Premises | 4,660 |
| Menu items | 2,647 |
| Menu item confidence | 56 verified / 2,585 estimated / 6 community |
| GroceryProduct rows | 19 (18 estimated, 1 community, 0 verified) |
| Trusted (verified/community) items by brand | McDonald's: 53, Bengawan Solo: 5, Cheers: 1, SaladStop!: 1, Paris Baguette: 1, Ichiban Boshi: 1 — 62 total, same composition as the last three runs |

## Theme-by-theme check

1. **`fastfood_protein_per_dollar`** — still blocked by the 14-day
   non-repeat rule. Posted 2026-08-31 to r/SGExercise (McDonald's-only, all
   50 verified items at the time); today is day 13 of that window, which
   clears **2026-09-14 (tomorrow)**. Left `status: pending` (the original
   cross-chain version remains unfulfilled — only McDonald's and 1
   Ichiban Boshi item are non-estimated across the whole fast-food/casual-
   dining set: 54 of 460 candidate items).

2. **`hawker_protein_under_5`** — re-checked fresh. 437 hawker menu items
   priced ≤$5 with protein > 0, **all 437 `estimated`, 0 verified/community**
   (was 426 on 2026-08-31, 437 on 2026-09-07 — flat since last run). Skipped.
   Left `pending`.

3. **`bubble_tea_calorie_ranking`** — re-checked fresh. Same 8 chains (Gong
   Cha, LiHo, KOI Thé, Chagee, Mixue, Hollin, R&B Tea, Taiwan Dessert & Milk
   Tea), 52 menu items, **all 52 `estimated`.** Unchanged since 2026-09-03.
   Skipped. Left `pending`.

4. **`grocery_protein_per_dollar`** — re-checked. 19 `GroceryProduct` rows,
   **18 `estimated`, 1 `community`, 0 verified/official** — unchanged since
   2026-09-03. Packaged SKUs carry a printed label, so an estimated number
   here is an easy public catch. Skipped. Left `pending`.

5. **`cheapest_150g_protein_day`** — re-checked. Still only 62 MenuItems
   database-wide with `verified`/`community` confidence, same 6-brand
   composition as the last two runs (53 McDonald's, 5 Bengawan Solo, 1 each
   Cheers/SaladStop!/Paris Baguette/Ichiban Boshi). Any 3-meal combo hitting
   150g protein from this pool still leans on McDonald's for most of the
   day — undercuts the theme's own "eating out generally" premise. Skipped.
   Left `pending`.

6. **`newly_added_chain_spotlight`** — checked the most recent real
   additions per `git log`: Lixin Teochew Fishball Noodle, Malalah, Rong
   Cheng Rou Gu Cha, Tartini Grill & Pasta, and Hjh Maimunah (5 related
   brand records) — all at The Clementi Mall, added 2026-09-11 to
   2026-09-13. **Every menu item across all of these is `confidence:
   "estimated"`** (individual hawker-stall research, no official nutrition
   source exists for any of them). Doesn't meet the public-claim bar.
   Skipped. Left `pending`.

7. **`mrt_line_protein_map`** — re-confirmed structurally unrunnable:
   `MenuItem` (`src/types/db.ts`) still has no per-`Premises` price/macro
   field — `price`/`calories`/`protein`/`carbs`/`fat` are keyed to `brandId`
   only, so there's no real geographic price variance within the trusted
   subset to rank by area. Unchanged since 2026-09-03. Skipped. Left
   `pending`.

## Root cause (unchanged, now a fourth consecutive confirmation)

Same bottleneck as every prior run: **McDonald's is still effectively the
only chain with broad `verified` macro coverage** (53 of 56 verified
MenuItems, plus 1 Ichiban Boshi item). Recent database growth (Clementi
Mall hawker stalls, Hjh Maimunah, etc.) is real and useful for app coverage
but is all individual hawker-stall research — `estimated` confidence by
nature, since no official nutrition source exists for these stalls. It does
nothing to move any of the blocked themes, which all depend on having a
second or third chain with real published macros.

## Suggested next steps for whoever reviews this

- No action needed on Reddit — nothing was drafted or typed anywhere this
  run.
- The `fastfood_protein_per_dollar` 14-day window clears **2026-09-14**
  (tomorrow) — the next run on or after that date can reuse the McDonald's
  data with a fresh angle (e.g. a specific menu category, or an update if
  McDonald's SG has changed prices/items) if nothing else has cleared by
  then.
- The durable fix is unchanged from the last three digests: sourcing an
  official nutrition-PDF/HPB-backed macro dataset for 2-3 more major chains
  (KFC and Burger King remain the best candidates) is the one input every
  blocked theme is waiting on.

**Reminder:** this run did not open a browser tab or contact Reddit in any
way — there is nothing pending for a human to review or post.
