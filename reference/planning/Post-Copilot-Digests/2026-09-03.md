# Post Copilot Digest — 2026-09-03

## Outcome: no post drafted this run

Every theme in `CONTENT_QUEUE.md` was checked against fresh live data. None
cleared the "8-10 solid, publicly-defensible rows" bar, and the one theme
that cleared it in the last run (`fastfood_protein_per_dollar`, narrowed to
McDonald's-only) is now off-limits for reuse under the 14-day non-repeat
rule. Rather than force a thin post or re-post the same McDonald's data
under a different label, this run stops here with no draft and no browser
tab opened. Nothing was typed into Reddit.

## Live data snapshot (via scratch `npx tsx` script against `src/lib/*.ts`)

| Metric | Value |
|---|---|
| Brands | 1,717 |
| Premises | 4,655 |
| Menu items | 2,563 |
| Menu item confidence | 56 verified / 2,501 estimated / 6 community |
| GroceryProduct rows | 19 (18 estimated, 1 community, 0 verified) |

## Theme-by-theme check

1. **`fastfood_protein_per_dollar`** — the last run (2026-08-31, 3 days ago)
   already drafted this theme (narrowed to a McDonald's-only version,
   posted to r/SGExercise). That's inside the 14-day non-repeat window, so
   per the rotation rule this theme is skipped this run without
   re-checking its numbers — reusing the identical underlying data under a
   different headline would just be a near-duplicate post to the same
   audience pool within a week. `status` left `pending` in the queue
   (unchanged — the *original* cross-chain version of this theme still
   isn't fulfilled, same reasoning as 08-31's digest).

2. **`hawker_protein_under_5`** — re-checked fresh. 687 total hawker menu
   items; 426 priced ≤$5 with protein > 0; **all 426 are `estimated`, 0
   `verified`/`community`.** Identical finding to 2026-08-31 (was 436/0
   then; item count shifted slightly from unrelated database churn but the
   confidence picture is unchanged). Skipped — 0 qualifying rows, nowhere
   near the 8-10 minimum. Left `pending`.

3. **`bubble_tea_calorie_ranking`** — re-checked fresh. 8 bubble tea brands
   (Gong Cha, LiHo, KOI Thé, Chagee, Mixue, Hollin, Taiwan Dessert & Milk
   Tea, R&B Tea), 52 menu items, **all 52 `estimated`.** Same fact-check-risk
   reasoning as 08-31 (calorie counts on bubble tea are exactly the kind of
   number a regular customer would compare against the chain's own posted
   figures). Skipped. Left `pending`.

4. **`grocery_protein_per_dollar`** — checkable for the first time this run
   (`GROCERY_PRODUCTS` went from 0 rows on 08-31 to 19 rows as of
   2026-08-31's ingredient-migration work). Checked the confidence
   breakdown of those 19 rows: **18 `estimated`, 1 `community`, 0
   `verified`/official.** Packaged supermarket SKUs are printed with an
   actual nutrition label, making an `estimated` macro number on a FairPrice
   item one of the easiest possible claims for a Redditor to catch as wrong
   — a worse fact-check-risk profile than even the fast-food theme, not a
   better one. Skipped — doesn't meet the "genuinely trustworthy for a
   public claim" bar despite now technically having enough rows. Left
   `pending`.

5. **`cheapest_150g_protein_day`** — re-checked fresh. Only 62 MenuItems
   database-wide carry `verified`/`community` confidence: McDonald's (53),
   Bengawan Solo (5, desserts), Cheers (1), SaladStop! (1), Paris Baguette
   (1), Ichiban Boshi (1). Same structural problem as 08-31: assembling 3
   diverse "eating out" meals hitting 150g protein from this pool still
   means leaning on McDonald's for the majority of the day, which undercuts
   the theme's own "cheapest realistic day of eating out" premise (it would
   really be "eat McDonald's three times"). Skipped. Left `pending`.

6. **`newly_added_chain_spotlight`** — checked the most recent real chain
   addition, `eighteen_chefs` (added earlier today, 2026-09-03, per
   `reference/research-sessions/2026-09-03-eighteen_chefs.md`). All 6 of its
   menu items are `confidence: "estimated"` (no official nutrition PDF or
   HPB entry exists for this chain — prices were cross-verified across menu
   aggregators, but macros are interpolated from similar dishes elsewhere in
   the DB) and it has **zero Premises** (no verified address/geocode yet, so
   there isn't even a "near you" hook). Doesn't meet the "genuinely
   trustworthy for a public claim" bar for a spotlight post. Skipped. Left
   `pending`.

7. **`mrt_line_protein_map`** — checked the schema itself rather than just
   the data: `MenuItem.price`/macros are keyed to `brandId`, not to an
   individual `Premises` (confirmed in `src/types/db.ts`) — a chain like
   McDonald's has one nationwide price per dish, not a price that varies by
   outlet. The only items with solid (`verified`/`community`) confidence are
   exactly these fixed-nationwide-price chain items, so there is currently
   no real geographic price variance in the trustworthy subset of the data
   to rank by MRT line/area at all — this theme is structurally
   unrunnable until hawker-stall-level data (which does vary
   stall-by-stall) reaches `verified`/`community` confidence. Skipped. Left
   `pending`.

## Root cause (unchanged from 2026-08-31, now compounding)

Every theme keeps failing on the same bottleneck: **McDonald's is still the
only chain with broad `verified` macro coverage** (53 of 56 verified
MenuItems). That was survivable last run because the McDonald's-only cut of
`fastfood_protein_per_dollar` hadn't been posted yet; this run, that exact
escape valve is now used up for the next ~11 days (14-day window from
2026-08-31), and nothing else in the queue has moved onto solid ground in
the meantime. Concretely: `hawker_protein_under_5` and
`bubble_tea_calorie_ranking` are still 100% estimated, the newly-populated
`GroceryProduct` rows are 95% estimated, and the one brand-new chain added
today (`eighteen_chefs`) is fully estimated with no address data. **No
theme-rotation choice fixes this — it needs more chains to get
officially-sourced (nutrition-PDF or HPB-backed) macro data, the same
recommendation as 08-31's digest, now with one additional cycle of evidence
that the queue has no other lever to pull.**

## Suggested next steps for whoever reviews this

- No action needed on Reddit — nothing was drafted or typed anywhere.
- If a post is wanted again before more chains get verified data, the
  fastest realistic path is prioritizing verified-macro sourcing (official
  nutrition PDF / HPB Nutrition Information Centre lookup) for 2-3 more
  major chains — KFC and Burger King both publish this kind of information
  in some markets and are worth checking for an SG-specific version — since
  that's the one input every skipped theme above is actually blocked on.
- Alternatively, wait out the 14-day window on `fastfood_protein_per_dollar`
  and let the next run reuse that McDonald's data with a fresh angle if
  nothing else has cleared by then.

**Reminder:** this run did not open a browser tab or contact Reddit in any
way — there is nothing pending for a human to review or post.
