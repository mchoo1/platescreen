# Post Copilot Digest — 2026-09-17

## Outcome: no post drafted this run — every theme in rotation is currently blocked

## Theme selection & rotation check

Read `CONTENT_QUEUE.md` (all seven themes still `status: pending` — nothing
has been confirmed posted since this queue was created) and the last 14
days of `Post-Copilot-Digests/` (`2026-09-03`, `2026-09-07`, `2026-09-13`,
`2026-09-14`) before picking a theme, per this task's own instructions.

Rather than trust the prior digests' numbers as still current (data changes
daily from the three unattended research scheduled tasks), re-computed
everything live this run via a scratch script (`node --experimental-strip-types`
importing `brands.ts`/`menuItems.ts`/`premises.ts`/`groceryProducts.ts`
directly — the sandbox shell was available this run, unlike 2026-09-14).

## Live data snapshot (recomputed 2026-09-17)

| Metric | Value |
|---|---|
| Total brands | 1,727 |
| Total menu items | 2,668 |
| Total premises | 4,666 |
| Menu item confidence | 56 `verified` / 2,606 `estimated` / 6 `community` |
| Non-estimated menu items by brand | McDonald's 53, Bengawan Solo 5, Cheers 1, SaladStop! 1, Paris Baguette 1, Ichiban Boshi 1 |
| Hawker items ≤$5 with protein > 0 | 437 total, **0** non-estimated |
| Bubble tea chain items (6 chains found) | 36 total, **0** non-estimated |
| Grocery SKU confidence | 18 `estimated`, 1 `community`, 0 `verified` |

This matches the 2026-09-13/2026-09-16 findings almost exactly (verified
count unchanged at 56/53-McDonald's; estimated count grew slightly from
background research-task additions, all still `estimated`). **No new chain
has crossed into verified/community confidence since the last run.**

## Theme-by-theme rotation check

- `fastfood_protein_per_dollar` — the only theme that has ever cleared the
  8-10-row quality bar (McDonald's, 53 verified items). **But** this exact
  theme was already drafted twice recently: 2026-08-31 (full list,
  r/SGExercise) and 2026-09-14 (items ≤$5 cut, r/singaporefi) — only 3 days
  ago. Per this task's rotation rule ("if this exact theme was drafted very
  recently, skip to the next theme"), redrafting a third McDonald's-only cut
  3 days after the last one would read as farming the same 53-row dataset
  rather than fresh content, especially since the 2026-09-14 draft already
  used the natural "budget cut" angle to avoid repeating 08-31. No new
  McDonald's items were added this week to justify another angle. **Skipped
  for recency**, not for a data problem — this theme should be the first
  one tried again once ~2 more weeks have passed or new McDonald's items
  land.
- `hawker_protein_under_5` — 0/437 non-estimated. Still fails the bar (same
  finding as every prior run since 2026-08-31). Skipped.
- `bubble_tea_calorie_ranking` — 0/36 non-estimated. Still fails. Skipped.
- `grocery_protein_per_dollar` — 0 `verified` grocery SKUs (18 estimated, 1
  community). Still fails, and packaged SKUs are the worst case for an
  estimated number since readers can check the printed label. Skipped.
- `cheapest_150g_protein_day` — only 62 verified/community items exist
  db-wide, concentrated in McDonald's + a handful of desserts/single items
  from 5 other brands — same "eat McDonald's 3x" problem flagged every prior
  run. Skipped.
- `newly_added_chain_spotlight` — checked `researchQueue.ts`/`branchQueue.ts`
  for the most recent chain resolved by the research scheduled tasks; no
  newly-added chain this week has verified (non-estimated) nutrition data
  attached yet. Skipped.
- `mrt_line_protein_map` — structurally blocked as documented in
  `CONTENT_QUEUE.md` (chain menu items are priced at the brand level, not
  per-Premises, so there's no geographic price variance to rank by until
  hawker-stall-level data reaches solid confidence). Skipped.

## Decision: no post this run

Every theme either fails the 8-10-solid-row bar outright, or (in
`fastfood_protein_per_dollar`'s case) was drafted too recently to redraft
without repeating the same 53-item McDonald's dataset a third time in under
three weeks. Posting a thin or repetitive list would work against this
project's whole credibility pitch (see `POSITIONING.md`), so per this task's
own rule ("if genuinely unsure whether a computed number/theme is
trustworthy or fresh enough, leave it out and note it rather than
guessing"), nothing was drafted or typed into Reddit this run.

**Nothing to prep in a browser this run** — steps 4/5 of the task (Reddit
submit-flow prep) were skipped since there is no post to type in.

## What actually unlocks this (unchanged from 2026-09-16 ROADMAP note)

The real fix is sourcing verified/official nutrition data for 2-3 more fast
food chains (KFC and Burger King were suggested in `ROADMAP.md`) — that's a
`platescreen-research-restaurants` task outcome, not something this
drafting task can produce itself. Once a second chain crosses into
verified confidence, `fastfood_protein_per_dollar` becomes a genuine
cross-chain comparison instead of a McDonald's-only list, which both
clears the bar more comfortably and stops this task from re-mining the same
53 rows every run.

## Next run recommendation

Re-check confidence breakdown by brand first (same scratch-script method
above). If a second chain has crossed into verified/community confidence,
run `fastfood_protein_per_dollar` as a real cross-chain ranking. Otherwise,
`fastfood_protein_per_dollar`'s McDonald's-only cut will be far enough from
2026-09-14 to redraft (a new angle — e.g. breakfast-only or drinks-only —
by ~2026-09-28, 14 days out).
